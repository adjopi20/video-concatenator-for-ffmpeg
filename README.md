# Video Segment Concatenator

A CLI tool to concatenate video segments into a single file.

## Installation

```sh
pip install -r requirements.txt
pip install .


## Prerequisites

Ensure `ffmpeg` is installed on your system. 

### On Ubuntu/Debian:
```sh
sudo apt update
sudo apt install ffmpeg